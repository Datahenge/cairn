# Reference

Command surface and file-format reference material will land here.

Until it's written, run `cairn --help` (or `cairn <command> --help`) for the current
command surface — it stays authoritative for the CLI's exact flags and behavior.
