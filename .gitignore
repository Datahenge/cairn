# NOTE: frappe_docker/ is intentionally NOT ignored — it is the ventwig-managed,
# pinned, drift-checked vendored copy of upstream, committed as plain files (D-007).

# Build/deploy artifacts
apps.json
compose.custom.yaml
*.env
!example.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# OS / editor
.DS_Store
*.swp
