---
status: authoritative
owner: technical
purpose: Current implementation inventory — what's built, where, how tested, what remains.
---

# Implementation Index

This is a **navigation and inventory system**, not a specification. Requirements documents
specify *what's needed* (`docs/requirements/`); ADRs and decisions explain *why* (`docs/adr/`,
`docs/decisions/`); this index answers *what exists now and where*.

**Completion is a judgment, not a grep.** Record a completion judgment in the table below the
moment one is reached — through testing, review, or discussion — even in a session with no
code change. Do not re-derive it from scratch in a later session; update this index instead.

**A cell is a verdict, not a narrative.** Say what's true now and point at `docs/CHANGELOG.md`
for the story of how it got there — don't re-narrate the incident, the bug, or the ADR inline.
See `01-documentation-conventions.md`'s "Completion Judgment Cells Stay Terse" for why this
keeps drifting and how to catch it in review.

> **Seeding caveat (2026-08-03):** this initial version is transcribed from
> `docs/archive/next-steps.md`'s already-written status notes (as of 2026-07-25) and updated only
> for the `cairn-build`/`cairn-adopt` binary split (`ADR-046`) and the `cairn-provision` →
> `setup` retirement. It is **not** a fresh audit of `src/cairn/` against current requirements.
> Treat the "Completion Judgment" column as provisional until re-verified in a follow-up
> session.

## Recommended Workflow

1. Read `ai/CURRENT_CONTEXT.md` and this index first in a fresh session.
2. Consult the owner documents named in the "Owner Docs" column for the relevant area.
3. Jump directly to the named code location and run the validation command.
4. Never infer completion from the `Status` column alone — read the "Completion Judgment" cell.

## Index

| Area | Requirement IDs | Status | Completion Judgment | Code Location | Tests / Validation | Owner Docs | Known Next Gap |
|---|---|---|---|---|---|---|---|
| Owned Docker recipe | `BR-VEND-*` | Implemented | Cairn owns its Docker build recipe outright (`ADR-059`, superseding `ADR-001`/`ADR-007`) — migrated from vendored `frappe_docker` (`W-023`..`W-031`), then trimmed to load-bearing content and flattened (`BR-VEND-005`, 2026-08-06). `ADR-068` grows scope to initial provisioning. Migration narrative: `docs/CHANGELOG.md` | `src/cairn/vendor.py`, `src/cairn/build.py`, `src/cairn/doctor.py`, `src/cairn/cli_build.py`, `src/cairn/images.py` | `tests/test_vendor.py`, `tests/test_build.py`, `tests/test_doctor.py`, `tests/test_cli_build.py`, `tests/test_images.py` | `docs/requirements/01-vendoring.md`, `docs/adr/068-cairn-owns-initial-provisioning-not-just-reconcile.md` | `W-032`/`W-033`, design-only |
| Build | `BR-BUILD-*`, `BR-CLI-002a` | Implemented | Working end-to-end against a real registry (2026-07-25). `build --push` assigns the declared environment by default since `ADR-066` (`--no-assign-tag` opts out) | `src/cairn/build.py`, `src/cairn/engine.py`, `src/cairn/resolve.py`, `src/cairn/appsjson.py`, `src/cairn/tagging.py`, `src/cairn/timing.py`, `src/cairn/transcript.py`, `src/cairn/cli_build.py` | `tests/test_build.py`, `tests/test_engine.py`, `tests/test_resolve.py`, `tests/test_appsjson.py`, `tests/test_tagging.py`, `tests/test_timing.py`, `tests/test_transcript.py`, `tests/test_cli_build.py` | `docs/requirements/02-build.md`, `docs/requirements/06-cli.md`, `docs/decisions/066-build-push-defaults-to-assign-tag.md` | — |
| Push / registry | `BR-CLI-003`, `BR-DEPLOY-004/005` | Implemented | Registry client working; retag path **never run against a real registry** | `src/cairn/push.py`, `src/cairn/registry.py` | `tests/test_push.py`, `tests/test_registry.py` | `docs/requirements/03-deploy.md` | First live retag/rollback against a real registry |
| Images / prune | `BR-CLI-005/018`, `BR-BUILD-018` | Implemented | `images`/`prune` verified on the real machine 2026-07-25. `cairn-build images` is local-only since `ADR-069` (2026-08-06) — registry introspection moved to `cairn-registry images` (`BR-REG-005` row below). `ADR-061` added a `cairn-build-owned` marker (`BR-BUILD-018`) so `prune` can safely reach a never-pushed stale build — resolves `OQ-001` | `src/cairn/images.py`, `src/cairn/prune.py`, `src/cairn/build.py`, `src/cairn/push.py`, `src/cairn/tagging.py` | `tests/test_images.py`, `tests/test_prune.py`, `tests/test_build.py`, `tests/test_push.py`, `tests/test_cli_build.py` | `docs/requirements/02-build.md`, `docs/requirements/06-cli.md`, `docs/adr/061-cairn-build-owned-marker-tag-untagged-only-pruning-retired.md`, `docs/decisions/069-images-splits-by-role-builder-local-registry-remote.md` | A real build+push+prune cycle verifying the marker lifecycle on an actual host |
| Reconcile / deploy | `BR-DEPLOY-*` | Verified live | Heavily tested and mutation-checked. First real target (2026-08-05) surfaced `W-022`: `running_digest()` confirmed only that a matching image existed locally, not that any container ran it, so an un-parameterized compose file produced a false `Converged`. Fixed — digest is now read off the running container itself; a mismatch is a loud `ReconcileError` (`BR-DEPLOY-003b`). Verified live afterward | `src/cairn/reconcile.py`, `src/cairn/descriptor.py`, `src/cairn/environments.py`, `src/cairn/config.py`, `src/cairn/build.py`, `src/cairn/cli_build.py` | `tests/test_reconcile.py`, `tests/test_descriptor.py`, `tests/test_environments.py`, `tests/test_config.py`, `tests/test_build.py`, `tests/test_cli_build.py` | `docs/requirements/03-deploy.md`, `docs/requirements/06-cli.md`, `docs/adr/052-manifest-environment-1-1-proof-not-assertion-promotion.md`, `docs/adr/021-deliberate-fork-of-frappe-docker-as-the-sanctioned-escape-hatch.md` | `setup-timer`/automation still unexercised on this target |
| Examine (formerly `adopt`) | `BR-CLI-020` | Written | Prints a descriptor read off a running stack. First real-world run (2026-08-04) surfaced three gaps, all fixed same session: a root-only `.env` crashed instead of reporting a `Finding`; a non-default compose filename was silently discarded (`Survey`/`Compose`/`descriptor.py` gained a `file` field); and a combined `<host>/<repo>` image string hid the registry host (split into `registry_host`/`image`, `ADR-057`/`ADR-058`). Narrative: `docs/CHANGELOG.md` | `src/cairn/adopt.py`, `src/cairn/descriptor.py`, `src/cairn/reconcile.py`, `src/cairn/registry.py` | `tests/test_adopt.py`, `tests/test_descriptor.py`, `tests/test_reconcile.py`, `tests/test_registry.py`, `tests/test_provision.py`, `tests/test_doctor.py`, `tests/test_cli_adopt.py` | `docs/requirements/03-deploy.md`, `docs/requirements/06-cli.md`, `userdocs/reference/target-descriptor.md`, `docs/decisions/058-target-descriptor-registry-host-is-required-docker-io-for-hub.md` | Real-world `examine`, descriptor install, and `reconcile` all verified live 2026-08-05 (see the Reconcile/deploy row for the caveat) — none unexercised any longer |
| Doctor | `BR-CLI-007` | Implemented, verified live | Role-specific checks per binary. `cairn-adopt doctor` ran clean 2026-08-05 against a real target VPS (`W-002`). Gained a live `github reachability` check 2026-08-06 (`ADR-067`) | `src/cairn/doctor.py` (`check_github_reachability`) | `tests/test_doctor.py` | `docs/requirements/06-cli.md`, `docs/adr/067-doctor-and-setup-timer-probe-github-reachability-before-trusting-it.md` | New `ADR-067` check unverified against a real private-app manifest |
| systemd units | `BR-CLI-019` | Implemented | Prints units, installs nothing | `src/cairn/systemd.py` | `tests/test_systemd.py` | `docs/requirements/03-deploy.md` | — |
| Setup (privileged installer) | `BR-DEPLOY-021` | Implemented, folded into all three CLIs | Was the standalone `cairn-provision`; now `cairn-build setup` / `cairn-adopt setup` / `cairn-registry setup` per `ADR-046`/`ADR-048` — generic engine (Runner, preflight, admin-group) extracted to `setup_runner.py` when the registry role split out — **never run on a real host** | `src/cairn/provision.py`, `src/cairn/setup_runner.py`, `src/cairn/registry_provision.py` | `tests/test_provision.py`, `tests/test_setup_runner.py`, `tests/test_registry_provision.py` | `docs/requirements/03-deploy.md`, `docs/requirements/08-registry.md` | First real-host `setup` run |
| Data-plane boundary | `BR-DATA-*` | Implemented (by construction) | No SQL client, no `bench execute`, no data-manipulation code path exists | (absence is the implementation — see `ADR-022`) | — | `docs/requirements/04-data.md` | — |
| Config | `BR-CFG-*` | Implemented | `/etc/cairn/` fully explicit, host-shared model (`ADR-042`) | `src/cairn/config.py` | `tests/test_config.py` | `docs/requirements/05-config.md` | — |
| GitHub auth (private apps) | `BR-BUILD-016` | Implemented | Shipped; PAT-based, redacted from output; missing-token hint on a failed lookup (point 5, added 2026-08-04) | `src/cairn/github_auth.py`, `src/cairn/resolve.py` | `tests/test_github_auth.py`, `tests/test_resolve.py` | `docs/requirements/02-build.md` | `BR-BUILD-017` local git mirror — planned, see `docs/plans/git-mirror-private-apps.md` |
| CLI surface (all three binaries) | `BR-CLI-001/002/021` | Implemented | `cairn-build`/`cairn-adopt`/`cairn-registry` three-entry-point split shipped (`ADR-046`, `ADR-048`) | `src/cairn/cli_build.py`, `src/cairn/cli_adopt.py`, `src/cairn/cli_registry.py`, `src/cairn/cli_support.py` | `tests/test_cli_build.py`, `tests/test_cli_adopt.py`, `tests/test_cli_registry.py` | `docs/requirements/06-cli.md` | — |
| Manifest home + setup-timer split | `BR-CLI-022/023` | Implemented | `cairn-build setup --client <name> --environment <name>` provisions `/srv/cairn/<name>/`, one manifest per environment (`ADR-052`). `setup-timer` derives client/image/environment from the manifest itself (never a flag), keyed off the full `(client, image_name, environment)` uniqueness key (`ADR-062`). Everything host-specific — script location, `WorkingDirectory=`, `EnvironmentFile=` — lives under the manifest's own `/srv/cairn/<client>/`, never the invoking shell's `cwd` (`ADR-064`, `ADR-065`). Gates on github reachability before writing (`ADR-067`); output noise/message bugs found and fixed 2026-08-06 (`W-013`). Narrative: `docs/CHANGELOG.md` | `src/cairn/provision.py` (`stage_manifest`, `manifest_template`, `build_script`, `build_unit_name`, `client_from_manifest`, `github_token_env_file`, `_check_timer_github_reachability`, `_timer_github_token`), `src/cairn/setup_runner.py`, `src/cairn/resolve.py`, `src/cairn/github_auth.py`, `src/cairn/doctor.py` (`check_known_manifests`), `src/cairn/cli_build.py`, `src/cairn/cli_adopt.py` | `tests/test_provision.py`, `tests/test_setup_runner.py`, `tests/test_resolve.py`, `tests/test_github_auth.py`, `tests/test_doctor.py`, `tests/test_cli_build.py`, `tests/test_cli_adopt.py` | `docs/requirements/06-cli.md`, `docs/adr/047-canonical-manifest-home-srv-cairn-scaffolding-and-setup-timer.md`, `docs/adr/052-manifest-environment-1-1-proof-not-assertion-promotion.md`, `docs/adr/065-per-client-github-token-via-environmentfile-not-a-shared-token.md`, `docs/adr/067-doctor-and-setup-timer-probe-github-reachability-before-trusting-it.md`, `docs/decisions/062-build-timer-unit-name-and-script-key-off-the-manifests-client-home.md`, `docs/decisions/064-build-timer-workingdirectory-and-workdir-flag-both-corrected.md` | `setup --client` and the scaffolded manifest verified live 2026-08-04. `setup-timer`'s happy path (a working token file, or a public-only manifest) still unexercised against a real host (`W-013`) |
| Conventions guard | (project-wide) | Implemented | AST-based `BR`/`ADR` leakage guard, proven able to fail | `tests/test_conventions.py` | self | `docs/technical/00-coding-standards.md` | — |
| Published docs site | `BR-DOCS-*` | Implemented (lean scope) | Pipeline stood up; nav-restructuring migration complete (`CONFIGURATION.md`/`ABOUT_GHCR.md`/`ABOUT_REGISTRIES.md` retired into the new structure, `README.md` deduplicated). `userdocs/target/` written ahead of a live run (2026-08-04), same caveat as `registry/index.md` | `mkdocs.yml`, `userdocs/` | manual `mkdocs build` | `docs/requirements/07-docs.md` | Remaining `BR-DOCS-006` nav restructuring: none of the originally-named files remain — gap closed. `userdocs/target/` unverified against a real target — tracks `W-002`/`W-013` |
| Registry (third CLI) | `BR-REG-*`, `BR-CLI-024/025/026/027` | Implemented | `cairn-registry` split out of `cairn-build setup` (`ADR-048`, 2026-08-03): provisioning, lifecycle, introspection, retention (tag-shape only, never reads `[cairn] environment`), and gc. Verified live 2026-08-04 — provisioning runs cleanly against the client's test VPS; `prune`/`gc` remain unexercised against a real registry. Two dry-run-contract bugs found and fixed same day (an unconditional mutation/report during `--dry-run`); `images` output later regrouped by digest for clarity, then gained `--host`/`--namespace`/`--image` and provenance detail (`BR-REG-005`, `ADR-069`, 2026-08-06 — moved wholesale from `cairn-build`); `stage_timers_registry`'s script now writes to `PROJECT_DIR`, not the operator's `cwd` (`ADR-063`). Narrative: `docs/CHANGELOG.md` | `src/cairn/registry_config.py`, `src/cairn/registry_provision.py`, `src/cairn/registry_retention.py`, `src/cairn/cli_registry.py`, `src/cairn/images.py`, `src/cairn/setup_runner.py` (generic engine, also now used by `provision.py`) | `tests/test_registry_config.py`, `tests/test_registry_provision.py`, `tests/test_registry_retention.py`, `tests/test_cli_registry.py`, `tests/test_images.py`, `tests/test_setup_runner.py` | `docs/requirements/08-registry.md`, `docs/adr/048-cairn-registry-a-third-cli-for-local-registry-lifecycle.md`, `docs/decisions/063-registry-maintenance-script-moves-to-opt-cairn-registry.md`, `docs/decisions/069-images-splits-by-role-builder-local-registry-remote.md` | First real `prune`/`gc` run against a live registry (`W-015`, narrowed 2026-08-05 — `setup` itself already verified live); `setup-timer` itself, including `ADR-063`'s fix, and the new `images --host`/exact-repository path against a real GHCR remote, still unexercised against a real host |

## Cross-cutting gap

**Everything in the deploy path is unexercised against real infrastructure** as of this
seeding. It is heavily tested and mutation-checked, which establishes that it does what it was
written to do — not that what it was written to do is right. The first live run is the test
that matters; see `docs/open/OPEN_WORK.md` for the ordered sequence.
