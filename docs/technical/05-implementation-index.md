---
status: authoritative
owner: technical
purpose: Current implementation inventory — what's built, where, how tested, what remains.
---

# Implementation Index

This is a **navigation and inventory system**, not a specification. Requirements documents
specify *what's needed* (`docs/requirements/`); ADRs and decisions explain *why* (`docs/adr/`,
`decisions/`); this index answers *what exists now and where*.

**Completion is a judgment, not a grep.** Record a completion judgment in the table below the
moment one is reached — through testing, review, or discussion — even in a session with no
code change. Do not re-derive it from scratch in a later session; update this index instead.

> **Seeding caveat (2026-08-03):** this initial version is transcribed from
> `docs/archive/next-steps.md`'s already-written status notes (as of 2026-07-25) and updated only
> for the `cairn-build`/`cairn-adopt` binary split (`ADR-046`) and the `cairn-provision` →
> `setup` retirement. It is **not** a fresh audit of `src/cairn/` against current requirements.
> Treat the "Completion Judgment" column as provisional until re-verified in a follow-up
> session.

## Recommended Workflow

1. Read `CURRENT_CONTEXT.md` and this index first in a fresh session.
2. Consult the owner documents named in the "Owner Docs" column for the relevant area.
3. Jump directly to the named code location and run the validation command.
4. Never infer completion from the `Status` column alone — read the "Completion Judgment" cell.

## Index

| Area | Requirement IDs | Status | Completion Judgment | Code Location | Tests / Validation | Owner Docs | Known Next Gap |
|---|---|---|---|---|---|---|---|
| Owned Docker recipe | `BR-VEND-*` | Implemented | Ownership model adopted 2026-08-05 (`ADR-059`, superseding `ADR-001`/`ADR-007`); `docs/requirements/01-vendoring.md` rewritten same day. Code migration (`W-023`..`W-031`) landed 2026-08-05: `src/cairn/vendored/` renamed to `src/cairn/recipe/`; `vendor.py` trimmed to `assert_build_inputs` (`BR-VEND-003`) plus a new `recipe_commit()` for provenance; `project.py` deleted entirely; `cli_build.py`'s `vendor` sub-app removed; `build.py`'s `plan()` drops the drift-gate calls, `provenance_labels()` now stamps cairn's own version + the recipe's git commit instead of an upstream pin; `doctor.py` dropped the two drift guards, kept the build-input one; `images.py`'s rendering follows suit (`recipe_commit` replacing `vendor_pin`); `pyproject.toml`'s `[tool.ventwig]` section and dev dependency removed; `userdocs/` swept of the `vendor status`/`sync` walkthrough and the `(ADR-001)` citation | `src/cairn/vendor.py`, `src/cairn/build.py`, `src/cairn/doctor.py`, `src/cairn/cli_build.py`, `src/cairn/images.py` | `tests/test_vendor.py`, `tests/test_build.py`, `tests/test_doctor.py`, `tests/test_cli_build.py`, `tests/test_images.py` | `docs/requirements/01-vendoring.md` | — |
| Build | `BR-BUILD-*`, `BR-CLI-002a` | Implemented | Working end-to-end against a real registry (2026-07-25). 2026-08-05 (`ADR-066`): `build --push` now assigns the declared environment by default (`--no-assign-tag` opts out); a manifest with none is skipped, not errored — only explicit `--assign-tag` still errors per `BR-CLI-009` | `src/cairn/build.py`, `src/cairn/engine.py`, `src/cairn/resolve.py`, `src/cairn/appsjson.py`, `src/cairn/tagging.py`, `src/cairn/timing.py`, `src/cairn/transcript.py`, `src/cairn/cli_build.py` | `tests/test_build.py`, `tests/test_engine.py`, `tests/test_resolve.py`, `tests/test_appsjson.py`, `tests/test_tagging.py`, `tests/test_timing.py`, `tests/test_transcript.py`, `tests/test_cli_build.py` | `docs/requirements/02-build.md`, `docs/requirements/06-cli.md`, `decisions/066-build-push-defaults-to-assign-tag.md` | — |
| Push / registry | `BR-CLI-003`, `BR-DEPLOY-004/005` | Implemented | Registry client working; retag path **never run against a real registry** | `src/cairn/push.py`, `src/cairn/registry.py` | `tests/test_push.py`, `tests/test_registry.py` | `docs/requirements/03-deploy.md` | First live retag/rollback against a real registry |
| Images / prune | `BR-CLI-005/018`, `BR-BUILD-018` | Implemented | `cairn-build images --local` and `prune` verified on the real machine 2026-07-25; registry-side `images` **never run against a real registry**. 2026-08-05 (`ADR-061`): new `cairn-build-owned` marker tag (`BR-BUILD-018`) applied at build time, stripped on a successful push; `prune` rewritten to reach a stale never-pushed image (not just duplicate-hash rebuilds) while still never touching anything pushed; `images --local` reports the marker per image, resolving `OQ-001`'s colocated-role ambiguity. Unit-tested only — not yet exercised on a real colocated host | `src/cairn/images.py`, `src/cairn/prune.py`, `src/cairn/build.py`, `src/cairn/push.py`, `src/cairn/tagging.py` | `tests/test_images.py`, `tests/test_prune.py`, `tests/test_build.py`, `tests/test_push.py`, `tests/test_cli_build.py` | `docs/requirements/02-build.md`, `docs/requirements/06-cli.md`, `docs/adr/061-cairn-build-owned-marker-tag-untagged-only-pruning-retired.md` | Registry-side `images` real-world verification; a real build+push+prune cycle verifying the marker lifecycle on an actual host |
| Reconcile / deploy | `BR-DEPLOY-*` | Verified live | Heavily tested and mutation-checked. 2026-08-05: first real target, a client VPS adopting a pre-existing hand-deployed site — `cairn-adopt reconcile` pulled, ran `compose up`, migrated, and reported `Converged`, but the target's `pwd.yml`-shaped compose file (fork-pressure register item 4, `ADR-021`) didn't consume `CUSTOM_IMAGE`/`CUSTOM_TAG` for any service, so nothing actually switched — `running_digest()` only confirmed a local image exists under the right tag, not that any container runs it, so the false convergence went unnoticed until checked by hand (`W-022`). Compose file hand-fixed to parameterize the image; a one-time manual `compose up`/`migrate` (bypassing `reconcile`'s now-satisfied short-circuit) brought `backend`'s actual running image ID to match the desired digest, confirmed via `docker inspect`. A subsequent real `cairn-adopt reconcile` correctly reported `Already running`. Same day: `W-022` fixed — `running_digest()` now reads the digest off the **running `backend` container's own image** (via `compose ps -q` → `docker inspect` → `docker image inspect`), not merely whether a matching image exists in the local store; a mismatch is a loud `ReconcileError`, never a silent false `Converged`. New requirement `BR-DEPLOY-003b` records this | `src/cairn/reconcile.py`, `src/cairn/descriptor.py`, `src/cairn/environments.py`, `src/cairn/config.py`, `src/cairn/build.py`, `src/cairn/cli_build.py` | `tests/test_reconcile.py`, `tests/test_descriptor.py`, `tests/test_environments.py`, `tests/test_config.py`, `tests/test_build.py`, `tests/test_cli_build.py` | `docs/requirements/03-deploy.md`, `docs/requirements/06-cli.md`, `docs/adr/052-manifest-environment-1-1-proof-not-assertion-promotion.md`, `docs/adr/021-deliberate-fork-of-frappe-docker-as-the-sanctioned-escape-hatch.md` | `setup-timer`/automation still unexercised on this target |
| Examine (formerly `adopt`) | `BR-CLI-020` | Written | Prints a descriptor read off a running stack. 2026-08-04: first real-world run, against a client VPS's existing deployment, crashed on `env_file.is_file()` — `pathlib` re-raises `EACCES` rather than swallowing it like a missing path, and an existing deployment's `.env` was root-only. Fixed: wrapped in `try`/`except OSError`, now reported as a `Finding` per the module's own "gaps are reported, never filled" rule instead of crashing. Same session, second gap: the deployment's base compose file was named `erpnext.yaml`, not `compose.yaml` — `examine` discarded the real filename (`Path(files[0]).parent` kept only the directory) even though `reconcile`'s compose invocation needed it. `Survey`/`Compose`/`descriptor.py` all gained a `file` field (`DEFAULT_COMPOSE_FILE = "compose.yaml"` when unset, backward compatible); `render()` now always prints it and `report()`'s "Compose files" line names the actual file, not just the directory. Same session, third gap (`ADR-057`, superseded hours later by `ADR-058`): `Descriptor.image` was a combined `<host>/<repo>` string — split into `registry_host` and `image` (repository path alone). New `Descriptor.repository` property (`registry_host/image`) feeds `reference`, `reconcile`'s `CUSTOM_IMAGE`, and its `RepoDigests` match; `registry.split_host()` added as `parse_ref`'s lenient sibling (no host is a fact, not an error). `ADR-057` made `registry_host` optional (absent = Docker Hub); `ADR-058`, same day, made it required instead once `registry.py`'s existing `_DOCKER_HUB_NAMES` showed `"docker.io"` was already the canonical explicit name — `split_host`'s hostless fallback returns `"docker.io"`, not `None` | `src/cairn/adopt.py`, `src/cairn/descriptor.py`, `src/cairn/reconcile.py`, `src/cairn/registry.py` | `tests/test_adopt.py`, `tests/test_descriptor.py`, `tests/test_reconcile.py`, `tests/test_registry.py`, `tests/test_provision.py`, `tests/test_doctor.py`, `tests/test_cli_adopt.py` | `docs/requirements/03-deploy.md`, `docs/requirements/06-cli.md`, `userdocs/reference/target-descriptor.md`, `decisions/058-target-descriptor-registry-host-is-required-docker-io-for-hub.md` | Real-world `examine`, descriptor install, and `reconcile` all verified live 2026-08-05 (see the Reconcile/deploy row for the caveat) — none unexercised any longer |
| Doctor | `BR-CLI-007` | Implemented, verified live | Role-specific checks per binary (`ADR-046` retired context-detection). `cairn-adopt doctor`'s target-role checks (descriptor, docker, compose, reconcile timer, registry, shared config) run clean 2026-08-05 against a real target VPS, both before and after descriptor install (`W-002`). 2026-08-06: gained a live `github reachability` check (`ADR-067`) | `src/cairn/doctor.py` (`check_github_reachability`) | `tests/test_doctor.py` | `docs/requirements/06-cli.md`, `docs/adr/067-doctor-and-setup-timer-probe-github-reachability-before-trusting-it.md` | New `ADR-067` check unverified against a real private-app manifest |
| systemd units | `BR-CLI-019` | Implemented | Prints units, installs nothing | `src/cairn/systemd.py` | `tests/test_systemd.py` | `docs/requirements/03-deploy.md` | — |
| Setup (privileged installer) | `BR-DEPLOY-021` | Implemented, folded into all three CLIs | Was the standalone `cairn-provision`; now `cairn-build setup` / `cairn-adopt setup` / `cairn-registry setup` per `ADR-046`/`ADR-048` — generic engine (Runner, preflight, admin-group) extracted to `setup_runner.py` when the registry role split out — **never run on a real host** | `src/cairn/provision.py`, `src/cairn/setup_runner.py`, `src/cairn/registry_provision.py` | `tests/test_provision.py`, `tests/test_setup_runner.py`, `tests/test_registry_provision.py` | `docs/requirements/03-deploy.md`, `docs/requirements/08-registry.md` | First real-host `setup` run |
| Data-plane boundary | `BR-DATA-*` | Implemented (by construction) | No SQL client, no `bench execute`, no data-manipulation code path exists | (absence is the implementation — see `ADR-022`) | — | `docs/requirements/04-data.md` | — |
| Config | `BR-CFG-*` | Implemented | `/etc/cairn/` fully explicit, host-shared model (`ADR-042`) | `src/cairn/config.py` | `tests/test_config.py` | `docs/requirements/05-config.md` | — |
| GitHub auth (private apps) | `BR-BUILD-016` | Implemented | Shipped; PAT-based, redacted from output; missing-token hint on a failed lookup (point 5, added 2026-08-04) | `src/cairn/github_auth.py`, `src/cairn/resolve.py` | `tests/test_github_auth.py`, `tests/test_resolve.py` | `docs/requirements/02-build.md` | `BR-BUILD-017` local git mirror — planned, see `docs/plans/git-mirror-private-apps.md` |
| CLI surface (all three binaries) | `BR-CLI-001/002/021` | Implemented | `cairn-build`/`cairn-adopt`/`cairn-registry` three-entry-point split shipped (`ADR-046`, `ADR-048`) | `src/cairn/cli_build.py`, `src/cairn/cli_adopt.py`, `src/cairn/cli_registry.py`, `src/cairn/cli_support.py` | `tests/test_cli_build.py`, `tests/test_cli_adopt.py`, `tests/test_cli_registry.py` | `docs/requirements/06-cli.md` | — |
| Manifest home + setup-timer split | `BR-CLI-022/023` | Implemented | `cairn-build setup --client <name> --environment <name>` provisions `/srv/cairn/<name>/`, scaffolds `cairn_<environment>.toml` only if absent (`ADR-052`: one manifest per environment, not a shared table); `setup-timer` takes `--manifest` only, deriving the environment from the file. Current unit name: `cairn-build-<client>-<image_name>-<environment>`, keyed off the full `(client, image_name, environment)` uniqueness key, not `environment` alone (`ADR-062`, fixing a cross-client collision risk). The generated script, its `cd`, and the unit's `WorkingDirectory=`/`EnvironmentFile=` (a per-client, optional GitHub-PAT path, `BR-BUILD-016`) all live under the manifest's own `/srv/cairn/<client>/` — never `options.workdir` (dropped from this command entirely) — so nothing depends on the invoking shell's `cwd` (`ADR-064`, `ADR-065`). `setup-timer` hard-stops if `--manifest` isn't canonically homed. The generated build script runs `build --push --assign-tag --yes` (`BR-CLI-002a`), then `prune --keep 1 --yes` (`ADR-051`); `doctor` lists known manifests and flags a duplicate `(image_name, environment)` declaration within a client. 2026-08-06: gates on github reachability before writing, refusing on failure (`ADR-067`) | `src/cairn/provision.py` (`stage_manifest`, `manifest_template`, `build_script`, `build_unit_name`, `client_from_manifest`, `github_token_env_file`, `_check_timer_github_reachability`, `_timer_github_token`), `src/cairn/setup_runner.py`, `src/cairn/doctor.py` (`check_known_manifests`), `src/cairn/cli_build.py`, `src/cairn/cli_adopt.py` | `tests/test_provision.py`, `tests/test_setup_runner.py`, `tests/test_doctor.py`, `tests/test_cli_build.py`, `tests/test_cli_adopt.py` | `docs/requirements/06-cli.md`, `docs/adr/047-canonical-manifest-home-srv-cairn-scaffolding-and-setup-timer.md`, `docs/adr/052-manifest-environment-1-1-proof-not-assertion-promotion.md`, `docs/adr/065-per-client-github-token-via-environmentfile-not-a-shared-token.md`, `docs/adr/067-doctor-and-setup-timer-probe-github-reachability-before-trusting-it.md`, `decisions/062-build-timer-unit-name-and-script-key-off-the-manifests-client-home.md`, `decisions/064-build-timer-workingdirectory-and-workdir-flag-both-corrected.md` | `setup --client` and the scaffolded manifest verified live 2026-08-04. `setup-timer` itself, including `ADR-062`/`ADR-064`/`ADR-065`/`ADR-067`'s corrections, still unexercised against a real host (`W-013`) |
| Conventions guard | (project-wide) | Implemented | AST-based `BR`/`ADR` leakage guard, proven able to fail | `tests/test_conventions.py` | self | `docs/technical/00-coding-standards.md` | — |
| Published docs site | `BR-DOCS-*` | Implemented (lean scope) | Pipeline stood up with placeholder content; `CONFIGURATION.md` (2026-08-04) and `ABOUT_GHCR.md`/`ABOUT_REGISTRIES.md` (2026-08-04, `ADR-054`) have completed their nav-restructuring migration; `README.md` swept of the content now duplicated. `docs/technical/CONFIGURATION.md`, `ABOUT_GHCR.md`, and `ABOUT_REGISTRIES.md` are all retired. 2026-08-04: `userdocs/target/index.md` + `automation.md` written (the third role's walkthrough, previously "Not yet written") from `BR-DEPLOY-*`/`BR-CLI-008/019/020/021/023` and the actual `cli_adopt.py`/`adopt.py`/`reconcile.py`/`systemd.py`/`provision.py` behavior — written ahead of a live run, same caveat as `registry/index.md` | `mkdocs.yml`, `userdocs/` | manual `mkdocs build` | `docs/requirements/07-docs.md` | Remaining `BR-DOCS-006` nav restructuring: none of the originally-named files remain — gap closed. `userdocs/target/` unverified against a real target — tracks `W-002`/`W-013` |
| Registry (third CLI) | `BR-REG-*`, `BR-CLI-024/025/026/027` | Implemented | `cairn-registry` split out of `cairn-build setup` (`ADR-048`) 2026-08-03: provisioning, lifecycle, introspection, retention (never reads `[cairn] environment` — protects by tag shape alone), and gc (read-only maintenance mode); mechanically verified to import neither `config.py` nor `environments.py` (`tests/test_registry_provision.py`). 2026-08-04: first `cairn-registry setup --dry-run` against a real Test VPS (`W-015`) surfaced two dry-run-contract bugs in `stage_registry` — `CERT_DIR.mkdir()` ran unconditionally instead of being gated on `runner.dry_run` (rule 2, a real filesystem mutation during a dry run), and `report.done.append(f"generated {crt}")` ran unconditionally too, so the summary falsely claimed `did: generated ...` when nothing had run (rule 6). Both fixed; `--workdir` also dropped from `cairn-registry setup` (kept on `setup-timer`) since none of `setup`'s three stages read it (`BR-REG-001`), and `Runner.run()` now prints the `what` label above each shell command it narrates. Same day, second slice: `setup` now scaffolds a starter `/etc/cairn/registry.toml` the first time none exists (`BR-REG-002a`, never touched again once present, `--force` included — mirrors `cairn-build setup`'s starter manifest), and a real full run (or `--only registry`) now finishes by running `doctor`'s checks and adopting its exit code (`BR-REG-003a`). Same day, a real (non-`--dry-run`) `cairn-registry setup` ran against the client's test VPS and completed cleanly — the registry role's provisioning path is verified live; `prune`/`gc` remain unexercised against a real registry. Same session as the `cairn-adopt` live-VPS fixes: `images`'s human-readable and `--json` output regrouped by digest instead of one row per tag (`BR-REG-005` updated), so every tag naming one build — content-hash tag, `latest`, any environment pointer — shows on one labeled line instead of three repeated, unlabeled digests; `--json`'s `images: [{digest, tags}]` replaces the flat `tags: [{tag, digest}]`, digest kept full (untruncated) for scripting. Same day, twice more: the repository line printed the bare name with no registry host, so a hand-copy into a target descriptor's `image` was actually incomplete — fixed by printing the full `<host>/<repo>` reference; then, once the descriptor gained a separate `registry_host` field (`ADR-057`), reverted to a bare `Repository <name>` line with `Registry <host>` printed once above it instead, matching the descriptor's own split rather than gluing the two back together. 2026-08-05 (`ADR-063`, companion to `ADR-062`): `stage_timers_registry` wrote its script to `options.workdir` (the operator's `cwd`) — now writes to `PROJECT_DIR` (`/opt/cairn-registry`), already provisioned for `compose.yaml`. No unit-naming fix needed — one registry per host |  `src/cairn/registry_config.py`, `src/cairn/registry_provision.py`, `src/cairn/registry_retention.py`, `src/cairn/cli_registry.py`, `src/cairn/setup_runner.py` (generic engine, also now used by `provision.py`) | `tests/test_registry_config.py`, `tests/test_registry_provision.py`, `tests/test_registry_retention.py`, `tests/test_cli_registry.py`, `tests/test_setup_runner.py` | `docs/requirements/08-registry.md`, `docs/adr/048-cairn-registry-a-third-cli-for-local-registry-lifecycle.md`, `decisions/063-registry-maintenance-script-moves-to-opt-cairn-registry.md` | First real `prune`/`gc` run against a live registry (`W-015`, narrowed 2026-08-05 — `setup` itself already verified live); `setup-timer` itself, including `ADR-063`'s fix, still unexercised against a real host |

## Cross-cutting gap

**Everything in the deploy path is unexercised against real infrastructure** as of this
seeding. It is heavily tested and mutation-checked, which establishes that it does what it was
written to do — not that what it was written to do is right. The first live run is the test
that matters; see `open/OPEN_WORK.md` for the ordered sequence.
