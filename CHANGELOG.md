# Changelog

`datahenge-cairn` is pre-1.0 and, for now, used locally by its author — release-by-release
changelog tracking starts at the first public **1.0.0** release, following
[Keep a Changelog](https://keepachangelog.com/) and [Semantic Versioning](https://semver.org/).

For pre-1.0 history, consult `git log`. Documentation, requirement, and decision revisions are
tracked continuously regardless of version in `docs/CHANGELOG.md` — see
`docs/technical/25-documentation-authority.md` for the full ownership map.
